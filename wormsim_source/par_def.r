# Default parameters for WORMSIM fit

  par.def <- list(surveys = list(start = list(year = 2011, month = 0),
                                 stop = list(year = 2015, month = 1),
                                 interval = list(year = 1, month = 0)))